# Conventions:
# - Number Keys: Per-game macros, do not override in rc file.
# - Fn Keys: Spells (see spell_fn_macros)

# "Prayer", first god ability.
macros += M p aa

# Recap items on current level.
# Ctrl+]
macros += M \{-3} *f@\{13}

# Display skill menu on game start.
{
    local need = true
    ready_r(function()
        if you.turns() == 0 and need and you.race() ~= "Gnoll" then
            need = false
            crawl.sendkeys("m")
        end
    end)
}
